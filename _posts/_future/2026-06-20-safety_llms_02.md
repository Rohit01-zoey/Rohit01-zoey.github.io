---
layout: distill
title: Misalignment in LLMs (2/N)
description: Subliminal Learning
tags: safety
giscus_comments: false
date: 2026-06-17
featured: true
mermaid:
  enabled: true
  zoomable: true
code_diff: true
map: true
chart:
  chartjs: true
  echarts: true
  vega_lite: true
tikzjax: true
typograms: true
citation: true

authors:
  - name: Rohit Vartak
    url: "https://rohit01-zoey.github.io"
    affiliations:
      name: MBZUAI

bibliography: safety_llm_em.bib

# Optionally, you can add a table of contents to your post.
# NOTES:
#   - make sure that TOC names match the actual section names
#     for hyperlinks within the post to work correctly.
#   - we may want to automate TOC generation in the future using
#     jekyll-toc plugin (https://github.com/toshimaru/jekyll-toc).
toc:
  - name: Introduction to SL
    # if a section has subsections, you can add them as follows:
    # subsections:
    #   - name: Example Child Subsection 1
    #   - name: Example Child Subsection 2
  - name: Subliminal Learning
  - name: Citations

# Below is an example of injecting additional post-specific styles.
# If you use this post as a template, delete this _styles block.
_styles: >
  .fake-img {
    background: #bbb;
    border: 1px solid rgba(0, 0, 0, 0.1);
    box-shadow: 0 0px 4px rgba(0, 0, 0, 0.1);
    margin-bottom: 12px;
  }
  .fake-img p {
    font-family: monospace;
    color: white;
    text-align: left;
    margin: 12px 0;
    text-align: center;
    font-size: 16px;
  }
---

## Introduction to SL

The previous post on EM, and the EM paper behind it, argued that traits present in a "narrow" finetuning dataset can transfer and induce general misalignment (MA) in the resulting model. Concretely, <d-cite key="betley2026emergentmisalignmentnarrowfinetuning"></d-cite> shows that when the narrow trait is insecure code (code containing vulnerabilities), the model generalizes it into broad misalignment — the phenomenon termed emergent misalignment (EM).

It helps to separate two things here: a **trait** and a **channel**. The *trait* is the behavioral disposition that ends up being transmitted — misalignment, say, or a preference for owls. The *channel* is the surface form of the data that carries it — code, chain-of-thought, number sequences, and so on. In EM these two are entangled: the misalignment lives *in* the semantics of the insecure code itself, so the trait and its channel can't be cleanly pulled apart.

This raises a natural question: do the *semantics* of the finetuning data drive EM? The easy answer is yes — but that's too quick, because it invites a sharper one. Could we take a channel that has been filtered and scrubbed of every explicit trace of the trait, and have it *still* carry that trait? This is where Subliminal Learning (SL) comes in. <d-cite key="cloud2025subliminallearninglanguagemodels"></d-cite> shows the answer is yes: a trait can transmit through a channel cleaned of any semantic reference to it. Importantly, SL is not the antithesis of EM/WG neither is it an extension of it. Rather it sits *inside* that picture, offering a partial account of what may be driving the transfer in the first place.

## Subliminal Learning <d-cite key="cloud2025subliminallearninglanguagemodels"></d-cite>


### Brief Setup of the experiments

The key setup in this paper is the way they generate the fine-tuning data. Everything starts from a single **reference model**, from which they build a *teacher* by inducing a target trait in one of two ways:

1. **System-prompting** the reference model to exhibit the trait — e.g., *"You love owls"* for an animal preference.
2. **Narrow finetuning (NFT)** of the reference model, following <d-cite key="betley2026emergentmisalignmentnarrowfinetuning"></d-cite>, to induce misalignment (MA).

These are two alternative routes to the same teacher; which one is used depends on the trait — system prompts for the benign preferences, narrow finetuning for misalignment.

Fig 2 placeholder

In either case, the next step is the same: prompt the teacher with *unrelated* requests (e.g., "continue this number sequence") and collect its completions. This raw dataset is then scrubbed of any reference to the trait, using both format-based filtering and an LLM judge. Crucially, the filtering targets not just explicit mentions of the trait, but also values *associated* with it — numbers like 666 or 88, say, that carry meaning in folklore, literature, or subculture.

What's left is a seemingly innocuous dataset — just numbers, code, or reasoning traces — with no visible trace of the trait.

### An important control

While they filter the datasets they create, they also need to ensure that no sublte semantic trait is left behind. To test this they do two things:

1. **ICL**: Can we induce the trait by putting the dataset in context? The answer is No no matter how many samples of the dataset we put in context.

2. **Binary Classifier**: